spring:
  application:
    name: contentservice #应用程序的名称，且必须直接映射到Spring Cloud Config 配置服务器中的目录名称
  profiles:
    active:
      default #指定服务应该运行环境配置profile
  cloud:
    config:
      uri: http://localhost:8888 # Cloud Config配置服务器端点及端点的位置指定Spring